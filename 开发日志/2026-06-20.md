# 开发日志 2026-06-20

## 完成事项

### GitHub 仓库搭建与自动部署配置

1. **本地仓库初始化**：创建 `.gitignore`，首次提交 58 个文件，推送到 `https://github.com/tangaoao/Decentralized-management.git`
2. **服务器 Git 环境**：`dnf install -y git` 安装 git
3. **WebHook 自动部署方案**：宝塔 WebHook 插件 + GitHub Webhook → push 自动触发 `git pull` + `npm run build` + `pm2 restart`
4. **文档沉淀**：
   - 新建 [docs/github-sync-setup.md](../docs/github-sync-setup.md) — 完整配置流程 + 踩坑记录 + 速查表
   - 更新 [CLAUDE.md](../CLAUDE.md) 标准文件索引
   - 更新 [docs/deployment-guide.md](../docs/deployment-guide.md) 日常更新章节

### 踩坑

| # | 现象 | 原因 | 解决 |
|---|------|------|------|
| 1 | `git push -u origin main` 报 `src refspec main does not match any` | 仓库无 commit，分支不存在 | 先 `git commit` 再 push |

## 待办

- [ ] 服务器端执行 git 初始化并与 GitHub 仓库关联
- [ ] 宝塔面板配置 WebHook 钩子
- [ ] GitHub 添加 Webhook 并验证自动部署链路
